# TODO

- [ ] Хранить refresh-токен в БД не в открытом виде, а в виде хеша. Сейчас поле `refresh_token` в модели пользователя и запись через DAO означают, что при утечке БД злоумышленник получит готовые refresh-токены. Безопаснее хранить хеш refresh-токена и сравнивать по нему.

- [ ] Исправить `logout`, чтобы он завершал refresh-сессию полностью. Сейчас удаляется только `users_access_token`, но `users_refresh_token` не очищается, и `refresh_token` в БД не инвалидируется. После logout пользователь всё ещё может получить новый access через `/refresh`.

- [ ] Исправить ротацию refresh-токена: перед обновлением сверять предъявленный `old_refresh` с токеном, который хранится у пользователя в БД. Сейчас `verify_and_update_refresh` просто находит пользователя по `user_id` и перезаписывает токен, поэтому старый украденный refresh-токен продолжает работать до истечения срока жизни.

- [ ] Удалить неиспользуемый параметр `user_data: Token` из ручки `/refresh`. Сейчас он не участвует в логике и делает контракт endpoint'а вводящим в заблуждение.

- [ ] Удалить мёртвый код `validate_token` из `security.py`. Сейчас функция больше не используется и только создаёт альтернативный, неактуальный путь валидации токенов.

- [ ] Привести `UserOut` к консистентному публичному контракту. Сейчас схема ответа не включает `name`, хотя модель пользователя требует это поле, из-за чего API регистрации и модель пользователя расходятся.
